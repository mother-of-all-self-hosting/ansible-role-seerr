# SPDX-FileCopyrightText: 2026 spatterlight
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Verify
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: Wait for seerr service to become active (up to 180s)
      ansible.builtin.systemd:
        name: seerr.service
      register: svc
      until: svc.status.ActiveState == 'active'
      retries: 36
      delay: 5

    - name: Assert seerr service is active
      ansible.builtin.assert:
        that: svc.status.ActiveState == 'active'
        fail_msg: "seerr.service not active ({{ svc.status.ActiveState }})"
        success_msg: "seerr.service is active"

    - name: Wait for seerr HTTP endpoint to respond (up to 10m)
      ansible.builtin.uri:
        url: "http://localhost:5055/"
        method: GET
        status_code: [200, 302]
        follow_redirects: none
      register: ep
      until: ep.status in [200, 302]
      retries: 120
      delay: 5

    - name: Assert seerr HTTP endpoint responds
      ansible.builtin.assert:
        that: ep.status in [200, 302]
        fail_msg: "seerr did not respond as expected ({{ ep.status | default('no response') }})"
        success_msg: "seerr responded with {{ ep.status }}"

    - name: Show service status summary
      ansible.builtin.debug:
        msg:
          - "seerr systemd: {{ svc.status.ActiveState }}"
          - "seerr HTTP: {{ ep.status }}"
