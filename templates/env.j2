{#
SPDX-FileCopyrightText: 2025 spatterlight
SPDX-FileCopyrightText: 2026 Slavi Pantaleev

SPDX-License-Identifier: AGPL-3.0-or-later
#}

TZ={{ seerr_environment_variables_tz }}
LOG_LEVEL={{ seerr_environment_variables_log_level }}

{# Seerr reads `PORT` (`const port = Number(process.env.PORT) || 5055`) and listens on 5055 without it. #}
{# Passing it keeps the port the container publishes, health-checks and advertises to Traefik in step with the port the process binds. #}
PORT={{ seerr_container_http_port }}

{{ seerr_environment_variables_additional_variables }}
